# 项目通用规则
#
# 此文件由 /devflow init 生成，你可以自由修改。
# DevFlow 的所有 Agent 在执行任务前都会读取此文件。
#
# 这里写的是项目特定的约定，可以补充或覆盖插件内置的默认规则。
# 插件内置规则位于 devflow/rules/ 目录（升级时自动更新），你无需修改它们。
# 只需要在下面写你项目的特殊约定——Agent 会合并两层规则。

## 项目概述
<!-- 简要描述项目是什么、做什么 -->

## 架构约定
<!--
示例：
- 后端采用 handler → service → repository 三层，handler 不直接查库
- 前端组件放在 src/features/ 下按功能分组
- API 统一返回 {code, message, data} 结构
-->

## 命名约定
<!--
示例：
- 后端文件蛇形命名，前端组件帕斯卡命名
- 数据库表加 t_ 前缀
- API 路径用 kebab-case
-->

## 目录结构
<!-- 描述关键目录的用途，帮助 Agent 理解代码组织 -->

## 通用禁止项
<!--
示例：
- 禁止在业务代码中直接写 SQL（必须通过 repository 层）
- 禁止在前端组件中直接 fetch（必须通过 api 层）
- 禁止提交 console.log / print 调试语句
-->

## 其他约定
<!-- 任何 Agent 应该知道的项目规则 -->
